dependencies:
  cache_directories:
    - typings

deployment:
  npm-publish:
    tag: /v[0-9]+(\.[0-9]+){2}(-.*[0-9]+)?/
    owner: palantir
    commands:
      - touch .npmrc
      - echo "\n$NPM_REGISTRY_CREDS\n" >> .npmrc
      - npm publish
